<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="hs_subscriptions_sync.list">
      <field name="name">hs_subscriptions_sync list</field>
      <field name="model">hs_subscriptions_sync.hs_subscriptions_sync</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="hs_subscriptions_sync.action_window">
      <field name="name">hs_subscriptions_sync window</field>
      <field name="res_model">hs_subscriptions_sync.hs_subscriptions_sync</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="hs_subscriptions_sync.action_server">
      <field name="name">hs_subscriptions_sync server</field>
      <field name="model_id" ref="model_hs_subscriptions_sync_hs_subscriptions_sync"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="hs_subscriptions_sync" id="hs_subscriptions_sync.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="hs_subscriptions_sync.menu_1" parent="hs_subscriptions_sync.menu_root"/>
    <menuitem name="Menu 2" id="hs_subscriptions_sync.menu_2" parent="hs_subscriptions_sync.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="hs_subscriptions_sync.menu_1_list" parent="hs_subscriptions_sync.menu_1"
              action="hs_subscriptions_sync.action_window"/>
    <menuitem name="Server to list" id="hs_subscriptions_sync" parent="hs_subscriptions_sync.menu_2"
              action="hs_subscriptions_sync.action_server"/>
    -->
  </data>
</odoo>